spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/SBJ_Project
    username: postgres
    password: R8$sP2KS
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
    open-in-view: false